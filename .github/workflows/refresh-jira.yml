name: Refresh Jira Data
on:
  # 6AM, 8AM, 10AM, 12M, 2PM, 4PM, 6PM Colombia (UTC-5 = 11,13,15,17,19,21,23 UTC)
  schedule:
    - cron: '0 11,13,15,17,19,21,23 * * 1-5'  # Lunes a Viernes
  # Manual trigger - ejecutar por comando
  workflow_dispatch:
permissions:
  contents: write
jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Run fetch
        env:
          JIRA_USER: ${{ secrets.JIRA_USER }}
          JIRA_TOKEN: ${{ secrets.JIRA_TOKEN }}
        run: python3 fetch_incidencias_en_curso.py
      - name: Push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add extracto-incidencias-en-curso.html incidencias_en_curso.json
          git diff --staged --quiet || (git commit -m "auto: refresh $(date -u +'%Y-%m-%d %H:%M UTC')" && git push)
